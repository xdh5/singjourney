{
  "name": "tone",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/wx",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run build:core && npm run dev -w @tone/web",
    "build:core": "npm run build -w @tone/pitch-core && npm run build -w @tone/curve-layout && npm run build -w @tone/contracts",
    "build:web": "npm run build:core && npm run build -w @tone/web",
    "build:wx": "npm run build:core && npm run build -w @tone/wx",
    "build": "npm run build:core && npm run build -w @tone/web && npm run build -w @tone/wx",
    "typecheck": "npx tsc -p apps/wx/tsconfig.json --noEmit && npm exec vue-tsc -- --noEmit -p apps/web/tsconfig.json",
    "test": "npm run test -w @tone/pitch-core"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vue-tsc": "^3.3.7"
  }
}
