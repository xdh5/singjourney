# 声刻度项目协作要求

本文件适用于整个仓库。修改代码前先阅读，并以用户最新明确要求为最高优先级；如果用户后续改变决定，应同步更新本文件。

## 沟通与执行

- 默认使用中文沟通，直接说明结果和关键问题，不堆砌术语。
- 开始工具操作前给出简短说明；耗时操作期间持续报告真实进度，不要无故停住。
- 用户要求修改时直接实施合理方案，不反复确认已经明确的事项。
- 不要在尚未完成构建时声称“已经写完”。
- 修改完成后执行对应 Docker 构建命令。除非排查问题确有必要，不在宿主机安装 npm、Python 等项目依赖。
- 不需要为了旧实现保留兼容层；确认无用、废弃或重复的代码应删除。
- 不覆盖或回退用户已有的无关改动。
- 用户明确要求推送时，检查范围后提交并推送到 GitHub；不要只给出命令让用户自己操作。
- 做任何技术或产品决定前，必须同时考虑实现所需的 token 成本和上线后的运营成本，包括服务器、存储、数据库、带宽、第三方 API、模型推理、维护和迁移成本。
- 如果多个方案效果接近，优先选择 token 消耗更少、长期运营成本更低、结构仍然清晰的方案；不得只考虑眼前开发方便。

## 强制编码规范

1. 后端 API 必须写好注释和接口文档：路由应说明用途、输入、输出、权限、错误情况和副作用；复杂业务逻辑应解释原因与边界，不能只重复代码表面含义。
2. 已有成熟开源库能够可靠解决的问题，一律优先使用开源库，不重复手写实现。在引入任何新库之前，必须先向用户说明用途、替代方案、许可证、包体积、性能影响、维护状态以及可能产生的服务器或商业授权成本，并取得用户明确同意；未获同意不得修改依赖文件或引入该库。
3. 不使用散落的魔法数字和魔法字符串。业务阈值、时间、大小限制、状态、平台标识、存储键、路由片段、颜色和重复文案等应集中定义为有明确名称的常量、枚举、配置或设计令牌，并按作用域统一管理。
4. 三端能够复用的类型、业务规则、状态转换、格式化、校验和工具函数必须放在共享模块中。平台差异只放在平台适配层，不得在多个页面复制同一套实现，也不得把通用 `utils` 分散到各个业务页面。
5. 新增抽象或工具前先搜索已有实现；优先扩展现有共享模块。不要创建含义模糊的万能 `utils` 文件，应按领域组织，例如音频、时间、录音、曲线、网络和存储。

## 产品范围

- 产品名称为“声刻度”，英文名称为“SingJourney”；用户界面中不要使用 `tone` 作为产品名。
- 客户端采用 uni-app 共享页面和业务代码，目标端包括 Web、两个微信小程序、iOS、Android 和 HarmonyOS。微信端拆为 `pitch-meter` 和 `vocal-practice` 两个独立小程序壳，但录音、Canvas、多语言、数据类型和平台适配仍复用同一套源码。
- `pitch-meter` 微信小程序只包含自由录音与录音记录；`vocal-practice` 微信小程序包含伴奏练声、练声录音和练声统计，并且登录页必须是首屏，未登录不能进入业务页。
- 所有平台的用户可见功能名称统一使用“音调仪”/“Pitch Meter”，不得再显示“自由录音”/“Free Recording”；该命名覆盖首页入口、录音页功能标签、空记录入口和默认录音名称。
- 能共用的页面、状态和数据结构必须共用；Canvas、录音、文件导出等平台能力允许使用独立适配层。
- 三端应保持主要 UI、功能和数据语义一致，平台权限、分享方式和本地存储提示按各端分别处理。
- iOS、Android 和 HarmonyOS 必须保持主要 UI、功能、交互流程和数据语义一致，并优先复用同一套页面、组件、状态与业务代码。不得为三个原生平台分别复制页面或形成互相漂移的实现；只有录音、音频播放、Canvas、文件、分享、权限、系统 API、签名打包和应用商店规则等确有平台差异的能力，才允许放入各自的平台适配层。新增或修改原生端功能时必须同时检查三个平台，不能只完成其中一端便视为完成。
- 客户端目前支持简体中文和英文，启动时根据系统语言自动选择；中文系统使用简体中文并显示产品名“声刻度”，其余语言默认使用英文并统一显示产品名“SingJourney”。所有用户可见文案集中维护，不在页面内散落语言判断。
- 当前记录以本地保存为主。不要在首页写“无账号无上传”“仅保存在当前微信”等宣传文案。
- 练声记录页必须按平台提示本地数据可能因清理浏览器/微信数据、卸载应用或更换设备而丢失，不要在 App 文案中写“小程序”。

## 视觉与交互

- 主视觉使用白色和项目既定的墨绿色 `#356b5b`，保持简洁、优雅，不擅自改成过深或其他主色。
- 音高 C1–C8 按八度分组显示不同配色，同组内由浅到深；C8 可以延续 C7 的视觉体系。
- 页面进出不要添加动画，录音工具栏也不要添加无意义动画。
- Web 录音页占满可视区域且不得显示无内容的页面滚动条；公开 URL 使用 History 路由并保持单层、语义清晰（`/`、`/record`、`/recordings`、`/practice`、`/share`），不得包含 `#`、`pages` 或重复层级。
- 自由录音与录音回放页的最顶部导航栏统一显示当前语言的产品名；画布左上角用较小的功能标签区分“自由录音”和“录音回放”，右上角保留保存、分享操作。
- 首页标题使用当前语言的产品名；首页两张入口卡片应与标题左右边缘对齐并充分利用屏幕宽度。
- 音调仪小程序首页采用本地打包的共享视觉资源和独立响应式布局，主入口突出自由录音，次入口为录音记录；页面底部必须保留明确的空白广告位。首屏固定视觉资源不放 OSS，未来可动态更换的广告与运营素材才使用远程存储。
- 录音页工具栏固定在底部，音调画布位于工具栏上方。
- 自由录音底部工具栏保留四项并等距居中：清空、播放、录音/暂停、下载。保存与分享位于录音页上方独立操作区；图标和文字不能溢出或漂移。
- AI 练声测评入口在录音暂停、结束或进入录音详情后显示，位于画布底部、工具栏上方；功能未实现前点击仅提示“AI测评待开发”，不得调用空接口或上传测评音频。
- 首页保留自由录音、本地录音记录、伴奏练声和练声统计入口。练声统计页面由多端共享，展示 GitHub 风格的每日练声热力图、今日各练声曲次数与时长，以及累计练声次数与时长；统计必须读取当前登录用户的真实练习数据，不得再使用演示数据。伴奏练声页面必须由多端共享，支持男声调/女声调和训练目标筛选，并明确展示固定音域、音型、推荐音节、强度与安全提示。
- 伴奏练声页面需提供两种未来的自定义伴奏方式：使用虚拟钢琴弹奏并保存，以及上传已有伴奏后由系统解析并转换成钢琴练声曲；每个练习模块提供试听入口。上述能力未实现前只展示共享假 UI 并提示待开发，不读取文件、不上传、不解析、不播放。
- 伴奏练声不提供用户起调升降：钢琴与音型规则统一覆盖 C3–F5，男声固定播放 C3–C5，女声固定播放 F3–F5。当前只开放第 3 个练习“Mum 八度连接”，其他练习保留展示但不可点击。
- 练习伴奏使用单音钢琴提示用户应唱旋律，不加入左手根音或和弦。钢琴音频与正确 Canvas 曲线必须从同一份服务端乐谱事件生成；默认练习在发布资产阶段预生成并缓存，不得在每次用户点击时重新合成。
- 练习开始后伴奏、录音、计时、目标曲线与人声曲线使用同一时间状态；伴奏结束自动封口录音，此后只能回放，不能继续录制。
- 伴奏音源尚未接入时，练习卡片不提供选中状态或曲目数量；保留“开始练习”按钮作为界面入口，但当前不绑定点击事件。页面上的男/女声、升降调和分类按钮必须真实改变当前配置或列表。
- 时长使用沙漏等明确的“持续时间”图标，不使用容易被理解为具体时刻的时钟图标。

## 录音、曲线与播放

- 单次录音最长 10 分钟。开始录音时提示时长上限，录至 9 分钟时只提示一次还剩 1 分钟，达到 10 分钟时自动结束并明确提示；客户端和分享 API 都必须校验该上限，不能只依赖界面限制。
- 录音和绘图必须保持流畅，尤其关注微信小程序在 iOS 真机上的性能；只渲染当前可见曲线区间，避免每帧重建整屏数据或频繁跨线程传输大对象。
- 录音时画布自动跟随当前时间和音高，纵向跟随应平滑；暂停或未录音时允许用户上下、左右拖动画布。
- 录音、播放声音、计时数字、曲线和竖向播放轴必须使用同一个时间状态，不能各自独立漂移。
- 伴奏练声必须通过统一的练习音频 Transport 调度：录音器先预热，伴奏在共享音频时钟的 `T0` 开始，正式录音数据也从 `T0` 开闸；回放时伴奏与人声必须在同一音频上下文按同一时刻启动，不得依赖两个播放器先后调用 `play()`，也不得用播放中的反复 `seek()` 补偿漂移。
- 伴奏练声中的录音按钮在伴奏未结束时只负责暂停和继续，不能提前把录音标记为停止；暂停录音后允许回放当前片段，回放结束后仍回到录音暂停状态。只有伴奏自然播放完成才停止录音，此后只允许回放，不能继续录制。
- 暂停录音后可以播放；继续录音时新旧录音应合并。
- 未手动拖动时，点击播放从 0 秒开始；手动拖动后从竖向时间轴所在位置开始。
- 停止录音时不立即跳回开头；点击首次播放时再平滑回到开头。第一次播放就必须同步更新播放图标、曲线、时间和播放轴。
- 播放过程中再次拖动后，必须从新的播放轴位置继续，而不是从上一次位置恢复。
- 已保存录音详情显示完整曲线，但不提供继续录制功能。
- 曲线以带时间、MIDI 音高和置信度的数据保存，不保存成截图；历史记录根据数据重新绘制。
- 录音名称使用“自由录音 + 语言无关日期时间”，避免“年/月/日”等中文日期单位。

## 音频格式与流量

- 当前微信小程序和 App 的本地录音为 16 kHz、单声道、16-bit WAV；Web 优先使用 Opus/WebM。
- 本地分析格式和网络传输格式分开设计，不为了上传压缩破坏实时音高分析。
- 分享试听目标格式为单声道 Opus，建议 48 kbit/s；普通音准类分析可使用 24–32 kbit/s Opus。
- CPP、HNR、H1-H2 等音质指标不能依赖低码率有损音频；需要时只上传短时持续元音的 FLAC，而不是整段 WAV。
- 优先减少用户上传流量和服务器存储，不在数据库中保存音频二进制。

## 服务端与数据

- 服务端使用 Python FastAPI，数据库使用 PostgreSQL，数据库变更必须通过 Alembic 迁移。
- 服务端按业务领域组织，不把所有路由和逻辑堆在单个文件中。
- 当前实际开放的后端业务包括临时录音分享、“练声相关”微信小程序的真实微信登录与应用会话，以及按登录用户记录和聚合的真实练声统计；云同步、在线伴奏和 AI 测评仍只规划清晰边界，不提前开放空接口。
- 练声统计只在练习自然完成时计一次，暂停和回放不得重复计数；客户端事件使用唯一 ID 幂等上传，离线失败时保留待同步队列。统计按用户本地时区聚合今天、最近 20 周和累计次数/时长，不上传录音或曲线。
- 分享音频是有过期时间的临时副本，默认 7 天；过期文件和元数据必须可定时清理。
- 音频文件通过存储适配层保存，PostgreSQL 只保存 `storage_key`、格式、大小、校验值等元数据。生产环境应可替换为 S3 兼容对象存储。
- 当前确定的生产部署边界：普通 API、PostgreSQL 与未来 AI 服务运行在阿里云；临时分享音频存放在 Cloudflare R2。客户端通过服务端签发的短期凭证直接上传和播放，音频流量不得经过 ECS 中转。
- 登录体系使用统一 `user_id`，手机号、Apple、微信等身份通过身份绑定表关联，不能为各端建立互不相通的用户体系。微信小程序必须由服务端使用 `wx.login` 的临时 code 换取身份并签发不透明应用会话，AppSecret 不得进入客户端。
- 本地录音只有在用户主动同步时才进入云端录音表；临时分享不能自动变成永久云端录音。
- AI 测评结果必须记录算法/模型版本、原始指标、分项得分和置信度，避免算法升级后无法解释历史结果。
- 当前唯一公开域名为 `singjourney.com`。客户端只允许在集中端点模块中定义该域名，业务页面不得写死域名；不提供运行时端点配置接口或旧域名兼容层。

## AI 练声测评边界

- 统一核心维度为：音准、气息表现、声音稳定、发声连贯、声音集中度。
- “气息表现”只根据乐句完成度、长音时长、断气、响度衰减和尾音稳定等可观测结果评分，不声称测出肺活量或呼吸方法。
- “声音集中度”可以根据 CPP、HNR、H1-H2 等估算气声和闭合倾向，但不能表述为医学意义上的“声带闭合程度”。
- 节奏不是所有练声的统一维度；仅在音阶、琶音等有目标时序的练习中作为“伴奏跟随”附加指标。
- 音域、颤音、换声断裂位置等作为专项数据，不强行塞入所有练习的统一总分。
- 不根据普通手机录音断言声带病变、喉部用力、呼吸方法正确、胸声/头声/混声或发声是否健康。

## Docker 与产物

- 客户端依赖和构建均在 Docker 中执行：
  `docker compose -f compose.client.yml run --rm --build client-build`
- 日常修改按影响范围执行单端 Docker 构建：`client-build-web`、`client-build-wx-pitch`、`client-build-wx-practice`、`client-build-ios` 或 `client-build-android`。只有共享核心改动、发布前验证或用户明确要求时才执行 `client-build` 全量构建。
- 客户端产物固定在：
  - `client/dist/h5`
  - `client/dist/mp-weixin/pitch-meter`
  - `client/dist/mp-weixin/vocal-practice`
  - `client/dist/ios`
  - `client/dist/android`
  - `client/dist/harmony`
- 原生端产物目录必须使用明确的英文平台名，不得使用 `app` 等泛称。HarmonyOS 最终工程和安装包由 HBuilderX 调用 DevEco Studio 工具链生成，再规范化到 `client/dist/harmony`。
- 微信开发者工具分别导入 `client/dist/mp-weixin/pitch-meter` 和 `client/dist/mp-weixin/vocal-practice`，不要再增加无意义的 `build` 或 `output` 中间目录。
- 服务端使用：
  `docker compose -f compose.server.yml up -d --build`
- Docker Desktop 当前配置的 `https://xxxx.mirror.aliyuncs.com/` 是无效占位地址。Python 基础镜像暂从公开 ECR 获取；不要重新依赖该无效镜像地址。
- 当前服务端容器应通过 `/health/live` 和 `/health/ready` 健康检查，启动时先执行数据库迁移。
