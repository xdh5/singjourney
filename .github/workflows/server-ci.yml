name: Server CI

on:
  pull_request:
    paths:
      - "server/**"
      - "Dockerfile.server"
      - "compose.server.yml"
      - ".github/workflows/server-ci.yml"
  push:
    branches: [main]
    tags:
      - "server-v*"
    paths:
      - "server/**"
      - "Dockerfile.server"
      - "compose.server.yml"
      - ".github/workflows/server-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: server-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: singjourney_ci
          POSTGRES_USER: singjourney_ci
          POSTGRES_PASSWORD: singjourney_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U singjourney_ci -d singjourney_ci"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      SINGJOURNEY_ENVIRONMENT: test
      SINGJOURNEY_DATABASE_URL: postgresql+psycopg://singjourney_ci:singjourney_ci@127.0.0.1:5432/singjourney_ci
      SINGJOURNEY_STORAGE_BACKEND: local
      SINGJOURNEY_STORAGE_ROOT: /tmp/singjourney-media
      SINGJOURNEY_PUBLIC_API_BASE_URL: https://example.test/api/v1
      SINGJOURNEY_PUBLIC_SHARE_BASE_URL: https://example.test
      SINGJOURNEY_CORS_ORIGINS: https://example.test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Validate Server release version
        shell: bash
        run: |
          tag=""
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then tag="$GITHUB_REF_NAME"; fi
          node ../scripts/release-version.mjs resolve server "$tag"
        working-directory: server

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: server/pyproject.toml

      - name: Install server dependencies
        working-directory: server
        run: python -m pip install --editable ".[dev]"

      - name: Compile Python sources
        working-directory: server
        run: python -m compileall -q app migrations tests

      - name: Apply database migrations
        working-directory: server
        run: alembic upgrade head

      - name: Run server tests
        working-directory: server
        run: pytest -q

  image:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build production server image
        run: docker build --file Dockerfile.server --tag singjourney-server:ci .

  deploy:
    needs: [verify, image]
    if: (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/server-v')) && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER || 'root' }}
      DEPLOY_PORT: ${{ vars.DEPLOY_PORT || '22' }}
      DEPLOY_PATH: ${{ vars.DEPLOY_PATH || '/opt/singjourney' }}
      DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY || secrets.ECS_SSH_KEY }}
      SINGJOURNEY_DATABASE_PASSWORD: ${{ secrets.SINGJOURNEY_DATABASE_PASSWORD }}
      SINGJOURNEY_R2_ACCOUNT_ID: ${{ vars.SINGJOURNEY_R2_ACCOUNT_ID }}
      SINGJOURNEY_R2_ACCESS_KEY_ID: ${{ secrets.SINGJOURNEY_R2_ACCESS_KEY_ID }}
      SINGJOURNEY_R2_SECRET_ACCESS_KEY: ${{ secrets.SINGJOURNEY_R2_SECRET_ACCESS_KEY }}
      SINGJOURNEY_R2_BUCKET_NAME: ${{ vars.SINGJOURNEY_R2_BUCKET_NAME || 'singjourney-share-audio' }}
      SINGJOURNEY_PUBLIC_API_BASE_URL: ${{ vars.SINGJOURNEY_PUBLIC_API_BASE_URL || 'https://singjourney.com/api/v1' }}
      SINGJOURNEY_PUBLIC_SHARE_BASE_URL: ${{ vars.SINGJOURNEY_PUBLIC_SHARE_BASE_URL || 'https://singjourney.com' }}
      SINGJOURNEY_CORS_ORIGINS: ${{ vars.SINGJOURNEY_CORS_ORIGINS || 'https://singjourney.com' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Validate deployment configuration
        run: |
          test -n "$DEPLOY_HOST" || { echo "Missing Actions variable: DEPLOY_HOST"; exit 1; }
          test -n "$DEPLOY_SSH_PRIVATE_KEY" || { echo "Missing Actions secret: DEPLOY_SSH_PRIVATE_KEY"; exit 1; }
          test -n "$SINGJOURNEY_DATABASE_PASSWORD" || { echo "Missing Actions secret: SINGJOURNEY_DATABASE_PASSWORD"; exit 1; }
          test -n "$SINGJOURNEY_R2_ACCOUNT_ID" || { echo "Missing Actions variable: SINGJOURNEY_R2_ACCOUNT_ID"; exit 1; }
          test -n "$SINGJOURNEY_R2_ACCESS_KEY_ID" || { echo "Missing Actions secret: SINGJOURNEY_R2_ACCESS_KEY_ID"; exit 1; }
          test -n "$SINGJOURNEY_R2_SECRET_ACCESS_KEY" || { echo "Missing Actions secret: SINGJOURNEY_R2_SECRET_ACCESS_KEY"; exit 1; }

      - name: Configure SSH
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "$DEPLOY_PORT" -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Package server release
        run: tar -czf server-release.tar.gz Dockerfile.server compose.server.yml server

      - name: Create protected server environment
        run: |
          umask 077
          encoded_database_password="$(python3 -c 'import os, urllib.parse; print(urllib.parse.quote(os.environ["SINGJOURNEY_DATABASE_PASSWORD"], safe=""))')"
          {
            printf 'SINGJOURNEY_ENVIRONMENT=production\n'
            printf 'SINGJOURNEY_DATABASE_PASSWORD=%s\n' "$SINGJOURNEY_DATABASE_PASSWORD"
            printf 'SINGJOURNEY_DATABASE_URL=postgresql+psycopg://singjourney:%s@db:5432/singjourney\n' "$encoded_database_password"
            printf 'SINGJOURNEY_STORAGE_BACKEND=cloudflare_r2\n'
            printf 'SINGJOURNEY_STORAGE_ROOT=/data/media\n'
            printf 'SINGJOURNEY_R2_ACCOUNT_ID=%s\n' "$SINGJOURNEY_R2_ACCOUNT_ID"
            printf 'SINGJOURNEY_R2_ACCESS_KEY_ID=%s\n' "$SINGJOURNEY_R2_ACCESS_KEY_ID"
            printf 'SINGJOURNEY_R2_SECRET_ACCESS_KEY=%s\n' "$SINGJOURNEY_R2_SECRET_ACCESS_KEY"
            printf 'SINGJOURNEY_R2_BUCKET_NAME=%s\n' "$SINGJOURNEY_R2_BUCKET_NAME"
            printf 'SINGJOURNEY_PUBLIC_API_BASE_URL=%s\n' "$SINGJOURNEY_PUBLIC_API_BASE_URL"
            printf 'SINGJOURNEY_PUBLIC_SHARE_BASE_URL=%s\n' "$SINGJOURNEY_PUBLIC_SHARE_BASE_URL"
            printf 'SINGJOURNEY_CORS_ORIGINS=%s\n' "$SINGJOURNEY_CORS_ORIGINS"
            printf 'SINGJOURNEY_DATA_PATH=%s/data\n' "$DEPLOY_PATH"
          } > server-deploy.env

      - name: Upload and activate server release
        run: |
          scp -P "$DEPLOY_PORT" -i ~/.ssh/deploy_key server-release.tar.gz server-deploy.env \
            "$DEPLOY_USER@$DEPLOY_HOST:/tmp/"
          ssh -p "$DEPLOY_PORT" -i ~/.ssh/deploy_key "$DEPLOY_USER@$DEPLOY_HOST" \
            "DEPLOY_PATH='$DEPLOY_PATH' RELEASE_SHA='$GITHUB_SHA' bash -s" <<'REMOTE'
          set -euo pipefail
          release_root="$DEPLOY_PATH/server/releases"
          release_path="$release_root/$RELEASE_SHA"
          current_path="$DEPLOY_PATH/server/current"
          environment_path="$DEPLOY_PATH/server/server.env"
          mkdir -p "$release_path" "$DEPLOY_PATH/data"
          tar -xzf /tmp/server-release.tar.gz -C "$release_path"
          rm -f /tmp/server-release.tar.gz
          install -m 600 /tmp/server-deploy.env "$environment_path"
          rm -f /tmp/server-deploy.env
          if [ -e "$current_path" ] && [ ! -L "$current_path" ]; then
            mv "$current_path" "$DEPLOY_PATH/server/pre-cd-$(date +%Y%m%d%H%M%S)"
          fi
          ln -sfn "$release_path" "$current_path"
          cd "$current_path"
          docker compose --project-name singjourney --env-file "$environment_path" \
            -f compose.server.yml up -d --build --remove-orphans
          for attempt in $(seq 1 18); do
            if docker compose --project-name singjourney --env-file "$environment_path" \
              -f compose.server.yml exec -T api python -c \
              "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/ready')"; then
              exit 0
            fi
            sleep 5
          done
          docker compose --project-name singjourney --env-file "$environment_path" \
            -f compose.server.yml logs --tail=100 api
          exit 1
          REMOTE
